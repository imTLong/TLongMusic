@model TLongMusic.Controllers.DailyNonstopsViewModel
@{
    ViewData["Title"] = "Kho Nonstop Theo Ngày - TLong Music Record Pool";
}

<div class="container px-lg-4 py-4">
    <!-- ==========================================
         HERO BANNER: KHO NONSTOP CẬP NHẬT THEO NGÀY
         ========================================== -->
    <div class="crystal-hero-stage mb-5">
        <div class="row align-items-center">
            <div class="col-lg-8">
                <div class="hero-pill-badge mb-3">
                    <i class="fas fa-fire-flame-curved text-warning"></i>
                    <span>NONSTOP RECORD POOL • BẢN MIX 30 - 90 PHÚT LIÊN TỤC</span>
                </div>

                <h1 class="crystal-hero-title mb-3">
                    KHO NONSTOP <br />
                    <span class="text-gradient-shimmer">CẬP NHẬT THEO TỪNG NGÀY</span>
                </h1>

                <p class="crystal-hero-desc mb-4">
                    Tuyển tập các bản Nonstop dài từ 30 đến 90 phút được mix liền mạch chuẩn âm thanh phòng thu, cập nhật mỗi ngày.
                    Phân định trực quan: <strong class="text-warning">👑 Nonstop Slot (Viền Vàng)</strong> &amp; <strong class="text-danger">💎 Nonstop Nhóm (Viền Đỏ)</strong>.
                </p>

                <!-- Legend phân biệt màu -->
                <div class="d-flex flex-wrap align-items-center gap-3 pt-2">
                    <div class="d-flex align-items-center gap-2 p-2 px-3 rounded-pill" style="background: rgba(255, 209, 102, 0.1); border: 1px solid rgba(255, 209, 102, 0.4);">
                        <span class="rounded-circle" style="width: 14px; height: 14px; background: #ffd166; box-shadow: 0 0 10px #ffd166;"></span>
                        <strong class="text-warning small">Nonstop Slot (Viền Vàng • WAV)</strong>
                    </div>
                    <div class="d-flex align-items-center gap-2 p-2 px-3 rounded-pill" style="background: rgba(255, 19, 74, 0.1); border: 1px solid rgba(255, 19, 74, 0.4);">
                        <span class="rounded-circle" style="width: 14px; height: 14px; background: #ff134a; box-shadow: 0 0 10px #ff134a;"></span>
                        <strong class="text-danger small">Nonstop Nhóm (Viền Đỏ • MP3)</strong>
                    </div>
                    <div class="d-flex align-items-center gap-2 p-2 px-3 rounded-pill" style="background: rgba(255, 255, 255, 0.05); border: 1px solid rgba(255, 255, 255, 0.15);">
                        <span class="rounded-circle" style="width: 14px; height: 14px; background: #8e9aaf;"></span>
                        <strong class="text-dim small">Nonstop Lọt (Miễn Phí • MP3)</strong>
                    </div>
                </div>
            </div>

            <div class="col-lg-4 d-none d-lg-flex justify-content-center">
                <div class="position-relative text-center">
                    <div class="p-4 rounded-circle d-inline-flex mb-3" style="background: radial-gradient(circle, rgba(255, 107, 139, 0.25) 0%, rgba(18, 19, 32, 0) 70%); border: 2px dashed rgba(255, 107, 139, 0.5);">
                        <i class="fas fa-fire-flame-curved fa-5x text-warning fa-bounce" style="animation-duration: 2.5s;"></i>
                    </div>
                    <div class="text-white fw-bold fs-5">@Model.TotalNonstops Bản Nonstop Tuyển Chọn</div>
                    <span class="badge badge-shimmer-ruby mt-1">Gom Nhóm Theo @Model.DailyGroups.Count Ngày</span>
                </div>
            </div>
        </div>
    </div>

    <!-- ==========================================
         THANH BỘ LỌC TÌM KIẾM THEO NGÀY & THỂ LOẠI
         ========================================== -->
    <div class="mb-5 p-3 rounded-4" style="background: rgba(18, 19, 32, 0.85); border: 1px solid rgba(255, 255, 255, 0.12); backdrop-filter: blur(20px); box-shadow: 0 10px 30px rgba(0,0,0,0.5);">
        <!-- Nút riêng biệt: Demo Nonstop Slot & Nonstop Nhóm & Nonstop Lọt -->
        <div class="d-flex flex-wrap align-items-center justify-content-between gap-2 mb-3 pb-3 border-bottom border-secondary border-opacity-25">
            <div class="d-flex flex-wrap align-items-center gap-2">
                <span class="text-dim small fw-bold me-1"><i class="fas fa-layer-group text-warning me-1"></i>Xem Nhanh:</span>
                <a href="/Nonstop" onclick="return applyNonstopTierFilter('all', event);" class="btn btn-sm btn-dark text-white rounded-pill px-3 py-1.5 fw-bold pill-all @(string.IsNullOrEmpty(Model.SelectedTier) || Model.SelectedTier == "all" ? "active" : "border-secondary")">
                    <i class="fas fa-layer-group me-1 text-warning"></i> Tất Cả
                </a>
                <a href="/Nonstop?tier=nhom" onclick="return applyNonstopTierFilter('nhom', event);" id="pillBtnNonstopNhom" class="btn btn-sm btn-dark rounded-pill px-3 py-1.5 fw-bold pill-nhom @(Model.SelectedTier == "nhom" ? "active" : "")">
                    <i class="fas fa-certificate text-danger me-1"></i> 💎 <span id="pillTextNonstopNhom">@(Model.IsVipUser ? "Nonstop Nhóm" : "Demo Nonstop Nhóm")</span>
                </a>
                <a href="/Nonstop?tier=slot" onclick="return applyNonstopTierFilter('slot', event);" id="pillBtnNonstopSlot" class="btn btn-sm btn-dark rounded-pill px-3 py-1.5 fw-bold pill-slot @(Model.SelectedTier == "slot" ? "active" : "")">
                    <i class="fas fa-crown text-warning me-1"></i> <span id="pillTextNonstopSlot">@(Model.IsPremiumUser ? "Nonstop Slot" : "Demo Nonstop Slot")</span>
                </a>
                <a href="/Nonstop?tier=lot" onclick="return applyNonstopTierFilter('lot', event);" class="btn btn-sm btn-dark rounded-pill px-3 py-1.5 fw-bold pill-lot @(Model.SelectedTier == "lot" ? "active" : "")">
                    <i class="fas fa-fire-flame-curved text-info me-1"></i> 🎵 Nonstop Lọt (Free)
                </a>
            </div>
            <div>
                @if (Model.SelectedTier == "slot")
                {
                    @if (!Model.IsPremiumUser)
                    {
                        <span id="badgeDemoNonstopSlotNotice" class="badge bg-warning text-dark fw-bold px-3 py-1.5 rounded-pill shadow">
                            <i class="fas fa-headphones me-1"></i> Đang hiển thị kho Demo Nonstop Slot (Nghe thử 30s)
                        </span>
                    }
                    else
                    {
                        <span id="badgeDemoNonstopSlotNotice" class="d-none"></span>
                    }
                }
                else if (Model.SelectedTier == "nhom")
                {
                    <span id="badgeNonstopNhomNotice" class="badge bg-danger text-white fw-bold px-3 py-1.5 rounded-pill shadow">
                        <i class="fas fa-certificate me-1"></i> @(Model.IsVipUser ? "Đang hiển thị kho Nonstop Nhóm Standard VIP" : "Đang hiển thị Demo kho Nonstop Nhóm (Cần VIP để tải)")
                    </span>
                }
                else if (Model.SelectedTier == "lot")
                {
                    <span class="badge bg-info text-dark fw-bold px-3 py-1.5 rounded-pill shadow">
                        <i class="fas fa-music me-1"></i> Đang hiển thị kho Nonstop Lọt (Miễn phí 100%)
                    </span>
                }
            </div>
        </div>

        <form method="get" asp-controller="Nonstop" asp-action="Index" class="row g-3 align-items-center">
            <!-- Tìm kiếm -->
            <div class="col-12 col-md-4">
                <div class="input-group input-group-sm">
                    <span class="input-group-text bg-dark border-secondary text-dim"><i class="fas fa-search"></i></span>
                    <input type="text" name="search" value="@Model.SearchQuery" class="form-control bg-dark text-white border-secondary" placeholder="Tìm theo tên bản Nonstop, DJ, thể loại..." />
                </div>
            </div>

            <!-- Lọc Thể loại -->
            <div class="col-6 col-md-3">
                <select name="genre" class="form-select form-select-sm bg-dark text-white border-secondary" onchange="this.form.submit()">
                    <option value="">Tất cả thể loại Nonstop</option>
                    @foreach (var g in Model.AvailableGenres)
                    {
                        <option value="@g" selected="@(Model.SelectedGenre == g ? "selected" : null)">@g</option>
                    }
                </select>
            </div>

            <!-- Lọc Phân Loại Bài -->
            <div class="col-6 col-md-3">
                <select name="tier" class="form-select form-select-sm bg-dark text-white border-secondary" onchange="this.form.submit()">
                    <option value="all" selected="@(Model.SelectedTier == "all" || string.IsNullOrEmpty(Model.SelectedTier) ? "selected" : null)">Tất cả phân loại</option>
                    <option value="slot" selected="@(Model.SelectedTier == "slot" ? "selected" : null)">👑 Chỉ Nonstop Slot (Viền Vàng)</option>
                    <option value="nhom" selected="@(Model.SelectedTier == "nhom" ? "selected" : null)">💎 Chỉ Nonstop Nhóm (Viền Đỏ)</option>
                    <option value="lot" selected="@(Model.SelectedTier == "lot" ? "selected" : null)">🎵 Chỉ Nonstop Lọt (Free)</option>
                </select>
            </div>

            <!-- Nút Lọc & Reset -->
            <div class="col-12 col-md-2 d-flex gap-2">
                <button type="submit" class="btn btn-sm btn-shimmer-ruby flex-grow-1 fw-bold">
                    <i class="fas fa-filter me-1"></i> Lọc
                </button>
                <a href="/Nonstop" class="btn btn-sm btn-outline-secondary px-3" title="Đặt lại bộ lọc">
                    <i class="fas fa-rotate-left"></i>
                </a>
            </div>
        </form>
    </div>

    <!-- ==========================================
         DANH SÁCH NONSTOP THEO TỪNG NGÀY (DAILY GROUPS)
         ========================================== -->
    @if (!Model.DailyGroups.Any())
    {
        <div class="p-5 text-center rounded-4 border border-secondary border-opacity-25" style="background: rgba(18, 19, 32, 0.6);">
            <i class="fas fa-music-slash fa-3x text-dim mb-3"></i>
            <h4 class="text-white fw-bold">Không tìm thấy bản Nonstop nào</h4>
            <p class="text-dim small mb-4">Vui lòng thử tìm kiếm với từ khóa khác hoặc bỏ các bộ lọc.</p>
            <a href="/Nonstop" class="btn btn-sm btn-shimmer-ruby px-4 py-2 rounded-pill fw-bold">
                <i class="fas fa-rotate-left me-1"></i> Xem Tất Cả Các Ngày
            </a>
        </div>
    }
    else
    {
        @foreach (var group in Model.DailyGroups)
        {
            <div class="daily-track-section mb-5" id="date-@group.Date.ToString("yyyy-MM-dd")">
                <!-- HEADER KHỐI NGÀY -->
                <div class="p-3 mb-3 rounded-4 d-flex flex-column flex-md-row align-items-start align-items-md-center justify-content-between gap-3"
                     style="background: linear-gradient(90deg, rgba(255, 107, 139, 0.15), rgba(18, 19, 32, 0.95)); border-left: 5px solid #ff6b8b; border: 1px solid rgba(255, 107, 139, 0.35); box-shadow: 0 10px 30px rgba(255, 107, 139, 0.08);">
                    <div class="d-flex align-items-center gap-3">
                        <div class="p-3 rounded-circle" style="background: rgba(255, 107, 139, 0.2); color: #ff6b8b;">
                            <i class="fas fa-calendar-day fa-2x"></i>
                        </div>
                        <div>
                            <div class="d-flex align-items-center gap-2">
                                <h2 class="h5 text-white fw-bold mb-0">Ngày @group.FormattedDate</h2>
                                <span class="badge bg-danger text-white fw-bold px-2 py-0.5" style="font-size: 0.72rem;">@group.DayOfWeekName</span>
                            </div>
                            <span class="text-dim small">Danh sách các bản Nonstop dài phát hành ngày @group.FormattedDate</span>
                        </div>
                    </div>
                    <div class="d-flex align-items-center gap-2">
                        <span class="badge badge-shimmer-ruby px-3 py-2 fw-bold font-monospace" style="font-size: 0.78rem;">
                            <i class="fas fa-fire-flame-curved me-1"></i> @group.Nonstops.Count Bản Nonstop
                        </span>
                    </div>
                </div>

                <!-- BẢNG NONSTOP TRONG NGÀY -->
                <div class="crystal-table-wrapper table-responsive @(Model.SelectedTier == "nhom" ? "filter-active-nhom" : (Model.SelectedTier == "slot" ? "filter-active-slot" : (Model.SelectedTier == "lot" ? "filter-active-lot" : "")))">
                    <table class="crystal-table">
                        <thead>
                            <tr>
                                <th style="width: 45px;"></th>
                                <th>Tên Nonstop / Tiêu Đề Bản Mix</th>
                                <th class="d-none d-md-table-cell">Thể Loại</th>
                                 <th class="d-none d-lg-table-cell">Thời Lượng</th>
                                 <th class="d-none d-sm-table-cell">Quyền Tải</th>
                                 <th class="d-none d-md-table-cell">Lượt Nghe</th>
                                 <th class="text-end" style="width: 100px;">Tải / Lưu</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var track in group.Nonstops)
                            {
                                var isNonstopSlot = (track.CategoryCode != null && track.CategoryCode.Contains("Slot", StringComparison.OrdinalIgnoreCase))
                                                 || track.TierRequiredToDownload == TLongMusic.Models.RequiredTier.Premium
                                                 || track.Type == TLongMusic.Models.AudioType.NonstopDat;
                                var isNonstopNhom = !isNonstopSlot && ((track.CategoryCode != null && track.CategoryCode.Contains("Nhom", StringComparison.OrdinalIgnoreCase))
                                                 || track.TierRequiredToDownload == TLongMusic.Models.RequiredTier.Standard);
                                var trackKind = isNonstopSlot ? "slot" : (isNonstopNhom ? "nhom" : "lot");
                                var shouldDemo = isNonstopSlot ? !Model.IsPremiumUser : (isNonstopNhom ? !Model.IsVipUser : false);

                                var rowClass = "crystal-table-row track-row-" + trackKind;
                                var coverImgClass = "rounded " + (isNonstopSlot ? "track-cover-slot" : (isNonstopNhom ? "track-cover-nhom" : ""));
                                var coverImgStyle = "width: 44px; height: 44px; object-fit: cover;" + (!isNonstopSlot && !isNonstopNhom ? " border: 1px solid rgba(255, 255, 255, 0.15);" : "");
                                var isWav = isNonstopSlot || (track.QualityAvailable != null && track.QualityAvailable.ToUpperInvariant().Contains("WAV"));
                                var cleanQuality = isWav ? "WAV" : "MP3";

                                <tr class="@rowClass"
                                    data-track-id="@track.Id"
                                    data-track-type="@trackKind"
                                    data-title="@track.Title"
                                    data-artist="@track.Artist"
                                    data-genre="@track.Genre"
                                    data-bpm="@track.Bpm"
                                    data-tier="@track.TierRequiredToDownload">
                                    <td>
                                        <button class="track-play-inline-btn"
                                                onclick='playTrack({
                                                    id: "@track.Id",
                                                    title: "@track.Title.Replace("\"", "\\\"")",
                                                    artist: "@track.Artist.Replace("\"", "\\\"")",
                                                    coverUrl: "@track.CoverUrl",
                                                    audioUrl: "@track.AudioUrl",
                                                    bpm: @track.Bpm,
                                                    key: "@track.MusicalKey",
                                                    durationSeconds: @track.DurationSeconds,
                                                    categoryCode: "@track.CategoryCode",
                                                    trackType: "@trackKind",
                                                    kind: "Nonstop",
                                                    isDemo: @(shouldDemo.ToString().ToLower()),
                                                    demoLimit: 30,
                                                    quality: "@cleanQuality",
                                                    tierRequired: "@track.TierRequiredToDownload"
                                                })'
                                                title="Phát bản Nonstop này">
                                            <i class="fas fa-play"></i>
                                        </button>
                                    </td>
                                    <td>
                                        <div class="d-flex align-items-center gap-3">
                                            <img src="@track.CoverUrl" alt="@track.Title" class="@coverImgClass" style="@coverImgStyle" />
                                            <div>
                                                <div class="track-title-wrapper d-inline-flex align-items-center flex-wrap">
                                                    <a href="/Track/Detail/@track.Id" class="text-white font-weight-bold track-title-link" style="font-size: 0.92rem; text-decoration: none;" title="Bấm để xem trang chi tiết tác phẩm">
                                                        @track.Title
                                                    </a>
                                                    @if (isNonstopSlot)
                                                    {
                                                        <span class="badge badge-premium-gold ms-2 fw-bold" style="font-size: 0.65rem; border: 1px solid #ffd166;" title="Nonstop Slot (Viền Vàng)"><i class="fas fa-crown me-1"></i>SLOT</span>
                                                    }
                                                    else if (isNonstopNhom)
                                                    {
                                                        <span class="badge badge-standard-red ms-2 fw-bold" style="font-size: 0.65rem; border: 1px solid #ff134a;" title="Nonstop Nhóm (Viền Đỏ)"><i class="fas fa-certificate me-1"></i>NHÓM</span>
                                                    }
                                                    else
                                                    {
                                                        <span class="badge bg-secondary ms-2" style="font-size: 0.65rem;" title="Nonstop Lọt Miễn Phí">LỌT</span>
                                                    }
                                                </div>
                                                <div class="text-dim small"><i class="fas fa-headphones text-danger me-1"></i> @track.Artist</div>
                                            </div>
                                        </div>
                                    </td>
                                    <td class="d-none d-md-table-cell">
                                        <span class="badge" style="background: rgba(255,255,255,0.06); border: 1px solid rgba(255,255,255,0.1); color: var(--text-silver); font-size: 0.72rem;">@track.Genre</span>
                                    </td>
                                    <td class="d-none d-lg-table-cell text-white small font-monospace">
                                        <i class="far fa-clock text-warning me-1"></i> @track.FormattedDuration
                                    </td>
                                    <td class="d-none d-sm-table-cell">
                                        @if (track.TierRequiredToDownload == TLongMusic.Models.RequiredTier.Premium)
                                        {
                                            <span class="badge badge-premium-gold"><i class="fas fa-crown me-1"></i> Premium</span>
                                        }
                                        else if (track.TierRequiredToDownload == TLongMusic.Models.RequiredTier.Standard)
                                        {
                                            <span class="badge badge-standard-red"><i class="fas fa-certificate me-1"></i> Standard</span>
                                        }
                                        else
                                        {
                                            <span class="badge bg-secondary">Free</span>
                                        }
                                    </td>
                                    <td class="d-none d-md-table-cell text-dim small">
                                        <i class="fas fa-headphones-simple me-1 text-dim"></i> @track.PlaysCount.ToString("N0")
                                    </td>
                                    <td class="text-end">
                                        <button class="btn btn-sm btn-link text-dim p-0 me-3" onclick="toggleFavorite('@track.Id', this)" title="Yêu thích">
                                            <i class="far fa-heart"></i>
                                        </button>
                                        @if (track.TierRequiredToDownload == TLongMusic.Models.RequiredTier.Premium)
                                        {
                                            <button class="btn btn-sm btn-download-premium px-3 py-1"
                                                    onclick="handleDownload('@track.Id', '@track.Title.Replace("'", "\\'")', '@track.TierRequiredToDownload')">
                                                <i class="fas fa-crown me-1"></i> Tải
                                            </button>
                                        }
                                        else if (track.TierRequiredToDownload == TLongMusic.Models.RequiredTier.Standard)
                                        {
                                            <button class="btn btn-sm btn-download-standard px-3 py-1"
                                                    onclick="handleDownload('@track.Id', '@track.Title.Replace("'", "\\'")', '@track.TierRequiredToDownload')">
                                                <i class="fas fa-arrow-down-to-bracket me-1"></i> Tải
                                            </button>
                                        }
                                        else
                                        {
                                            <button class="btn btn-sm btn-glass-crystal px-3 py-1"
                                                    onclick="handleDownload('@track.Id', '@track.Title.Replace("'", "\\'")', '@track.TierRequiredToDownload')">
                                                <i class="fas fa-arrow-down-to-bracket me-1"></i> Tải
                                            </button>
                                        }
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        }
    }
</div>
